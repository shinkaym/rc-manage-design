import { FeaturePlaceholderScreen } from '@/features/placeholders/feature-placeholder-screen';

export default function AboutUsRoute() {
  return (
    <FeaturePlaceholderScreen
      title="About Us"
      description="About Us is included now so the drawer mirrors Flutter exactly, even before the real content is designed."
    />
  );
}
